import { AddressService } from '../services/address.service';

describe('AddressService', () => {
    describe('save address', () => {
        it('if an correct address is sent it must be saved', async () => {
            expect(true).toBe(true);
        });
    });
});
